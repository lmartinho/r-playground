# Combine Values into a Vector or List
c <- c(7, 6, 3, 8, 6, 8, 7, 4, 4, 5, 8, 4) #, 9, 2)
# Converts to vector (looks the same)
v <- as.vector(c)
# Converts to data (now allow preview)
l <- as.list(c)

summary(v)
# TODO: How can we summarize the elements in the lists
summary(l)
